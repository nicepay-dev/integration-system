import { Body, Controller, Delete, Get, Param, Patch, Post, Query, Req, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { CreateMerchantDto, UpdateMerchantDto, UpdateProgressDto } from './merchant.dto'; import { MerchantsService } from './merchants.service';
@UseGuards(AuthGuard('jwt')) @Controller('merchants') export class MerchantsController { constructor(private service: MerchantsService) {} @Get() list(@Query('search') search?: string, @Query('status') status?: string) { return this.service.list(search, status); } @Get('summary') summary(){ return this.service.summary(); } @Get(':id/history') history(@Param('id') id:string){return this.service.historyFor(id);} @Get(':id') one(@Param('id') id:string){ return this.service.one(id); } @Post() create(@Body() dto:CreateMerchantDto,@Req() req:any){ return this.service.create(dto,req.user.name); } @Patch(':id') update(@Param('id') id:string,@Body() dto:UpdateMerchantDto,@Req() req:any){return this.service.update(id,dto,req.user.name);} @Patch(':id/progress') progress(@Param('id') id:string,@Body() dto:UpdateProgressDto,@Req() req:any){ return this.service.updateProgress(id,dto,req.user.name); } @Delete(':id') remove(@Param('id') id:string,@Req() req:any){return this.service.remove(id,req.user.name);} }
