import { Body, Controller, Get, Param, Patch, Post, Query, Req, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { CreateMerchantDto, UpdateProgressDto } from './merchant.dto'; import { MerchantsService } from './merchants.service';
@UseGuards(AuthGuard('jwt')) @Controller('merchants') export class MerchantsController { constructor(private service: MerchantsService) {} @Get() list(@Query('search') search?: string, @Query('status') status?: string) { return this.service.list(search, status); } @Get('summary') summary(){ return this.service.summary(); } @Get(':id') one(@Param('id') id:string){ return this.service.one(id); } @Post() create(@Body() dto:CreateMerchantDto){ return this.service.create(dto); } @Patch(':id/progress') progress(@Param('id') id:string,@Body() dto:UpdateProgressDto,@Req() req:any){ return this.service.updateProgress(id,dto,req.user.name); } }
