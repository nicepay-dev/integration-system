import { Body, Controller, Delete, Get, Param, Patch, Post, Query, Req, UseGuards } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport'; import { CreateLibraryProjectDto, UpdateLibraryProjectDto } from './project-library.dto'; import { ProjectLibraryService } from './project-library.service';
@UseGuards(AuthGuard('jwt')) @Controller('project-library') export class ProjectLibraryController { constructor(private service:ProjectLibraryService){} @Get() list(@Query('search') search?:string,@Query('status') status?:string,@Query('category') category?:string,@Query('picUserId') picUserId?:string){return this.service.list(search,status,category,picUserId);} @Get(':id/history') history(@Param('id') id:string){return this.service.historyFor(id);} @Post() create(@Body() dto:CreateLibraryProjectDto,@Req() req:any){return this.service.create(dto,req.user.name);} @Patch(':id') update(@Param('id') id:string,@Body() dto:UpdateLibraryProjectDto,@Req() req:any){return this.service.update(id,dto,req.user.name);} @Delete(':id') remove(@Param('id') id:string){return this.service.remove(id);} }
